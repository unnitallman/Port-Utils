name: PR-automerge

on:
  push:
    branches:
      - master

jobs:
  Auto:
    name: Auto-merge
    runs-on: ubuntu-18.04
    steps:
      - uses: unnitallman/pr-auto-merge@v1.0
        with:
          github_token: ${{ secrets.AUTOREBASE_TOKEN }}
