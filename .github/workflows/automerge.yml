name: PR-automerge

on:
  pull_request:
    types: [ opened, reopened, labeled, unlabeled ]

jobs:
  Auto:
    name: Auto-merge
    runs-on: ubuntu-18.04
    steps:
      - uses: unnitallman/pr-auto-merge@v0.3
        with:
          github_token: ${{ secrets.AUTOREBASE_TOKEN }}
