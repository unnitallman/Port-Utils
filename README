1. Check whether a given TCP port is free

PortUtils.port_free?(4300)


2. To get a free port from the ephemeral port range

PortUtils.get_free_port


3. To kill the process which is using (listening on) a particular port

PortUtils.kill(4300)
